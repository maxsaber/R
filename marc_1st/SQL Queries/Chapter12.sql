SELECT DISTINCT
    provnum,
    overall_rating,
    survey_rating,
    staffing_rating,
    RN_staffing_rating,
    quality_rating,
    ssf,
    restot,
    ownership
FROM
    nursing_home_provider_info;
